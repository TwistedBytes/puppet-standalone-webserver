---
# Hiera 5 Global configuration file

version: 5

# defaults:
#   data_hash: yaml_data
# hierarchy:
#  - name: Common
#    data_hash: yaml_data
hierarchy:
  - name: "Per-node data (yaml version)"
    path: "/etc/puppetlabs/code/hieradata/setup.yaml"